services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: art-gallery:local
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      HOSTNAME: 0.0.0.0
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
